service: hello-world

provider:
  name: aws
  runtime: nodejs12.x
  region: us-east-1
  stage: prod

functions:
  attributes:
    handler: handler.hello
    events:
      - http:
          path: who/{name}
          method: get
          cors: true